{
  "scripts": {
    "socket-server": "nodemon socket-server/src/server.ts",
    "client": "npm --prefix client run dev",
    "install": "npm --prefix client install && npm install && npm --prefix socket-server install"
  },
  "dependencies": {
    "nodemon": "latest",
    "socket.io": "^4.7.4"
  },
  "devDependencies": {
    "@types/node": "^20.11.5"
  }
}
